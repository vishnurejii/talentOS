# These will be overridden by Render's environment variable settings at build time.
# Render injects VITE_* vars during the build step automatically.
# Update these URLs after your first Render deployment to get the actual service URLs.

VITE_API_URL=https://talentos-backend.onrender.com/api
VITE_WS_URL=wss://talentos-realtime.onrender.com
